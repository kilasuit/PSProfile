Import-Module CompletionPredictor
Import-Module Microsoft.WinGet.CommandNotFound

# Think I have this elsewhere
#foreach ($file in Get-ChildItem -Path $($Profile.MyProfileDirectory)\src\completions\*\*.ps1 ) {
#     . $file.FullName
# }
